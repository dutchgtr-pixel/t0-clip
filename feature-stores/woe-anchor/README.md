# WOE Anchor Feature Store (Public)

Public, platform-agnostic assets for the WOE anchor store used during inference.
This store provides a lightweight, interpretable baseline signal derived from anchor-related variables.

See `sql/` for definitions and `docs/` for notes and verification queries.
