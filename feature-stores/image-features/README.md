# Image Feature Store (Public)

Public, platform-agnostic SQL and documentation for the image-derived feature store.
This store contains image feature definitions and related verification notes used downstream in modeling.

See `sql/` for definitions and `docs/` for notes and verification queries.
