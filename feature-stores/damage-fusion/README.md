# Damage Fusion Feature Store (Public)

Public, platform-agnostic SQL and documentation for the damage fusion feature store
(text + image fusion signals) used downstream in modeling.

See `sql/` for definitions and `docs/` for notes and verification queries.
